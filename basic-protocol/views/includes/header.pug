header.site-header
  a.brand(href='/verify') GovBind
  nav(aria-label='Main navigation')
    a(class=activePage === 'verify' ? 'active' : undefined href='/verify') Verify
    a(class=activePage === 'proof' ? 'active' : undefined href='/proof') Proofs
